using System;
using System.Collections.Generic;
using System.Text;

namespace ConsoleApp
{
    public enum typeDS
    {
        Arrays = 1,
        Exit = 2
    }

    public enum typeArrays
    {
        BasicArray = 'A',
        PrefixArray = 'B',
        CarryForwardArray = 'C',
        Exit = 'D'
    }
}
